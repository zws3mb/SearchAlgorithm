firstfreq<-read.table(file.choose())
lastfreq<-read.table(file.choose())
firstassoc<-read.table(file.choose())
lastassoc<-read.table(file.choose())
lastassoc<-read.table(file.choose())
View(lastassoc)
View(firstassoc)
lastassoc<-read.table(file.choose())
firstassoc<-read.table(file.choose())
View(firstassoc)
View(lastassoc)
plot(firstassoc)
plot(firstassoc[,2])
plot(sort(firstassoc[,2]))
plot(sort(lastassoc[,2]))
plot(lastassoc[,2])
firstassoc<-read.table(file.choose())
lastassoc<-read.table(file.choose())
View(lastassoc)
plot(sort(firstfreq[,2]))
plot(firstfreq)
plot(lastfreq)
plot(sort(lastfreq[,2]))
lastassoc<-read.table(file.choose())
View(lastassoc)
plot(firstassoc)
plot(firstassoc[,2])
plot(sort(firstassoc[,2]))
26*26
676^2
View(firstfreq)
plot(firstfreq[,2])
plot(firstfreq)
